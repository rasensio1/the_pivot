<div class="container">
  <div class="row">
    <div class="col-md-12">
      <% flash.each do |type, message| %>
        <div class="alert alert-<%= type %>" role="alert">
          <%= sanitize(message) %>
        </div>
      <% end %>
    </div>
  </div>
</div>
